# BackendService Configuration Example for FastAPI

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Database URL if using a database (optional, for future use)
# DATABASE_URL=sqlite:///./test.db

# OAuth2/JWT secrets or configuration variables (replace as per auth system)
# SECRET_KEY=<random_secret>
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Add any necessary environment variables here as needed
